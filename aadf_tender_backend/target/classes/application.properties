# ?? Datasource ???????????????????????????????????????????????????????????????
spring.datasource.url=jdbc:postgresql://localhost:5432/tender_db?sslmode=disable
spring.datasource.username=postgres
spring.datasource.password=mersi
spring.datasource.driver-class-name=org.postgresql.Driver

# ?? JPA / Hibernate ?????????????????????????????????????????????????????????
# Only create missing tables/columns; do not drop or recreate existing schema
spring.jpa.hibernate.ddl-auto=update
spring.jpa.generate-ddl=true      

# SQL logging
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Avoid session-open-in-view issues
spring.jpa.open-in-view=true

# ?? Jackson ?????????????????????????????????????????????????????????????????
spring.jackson.serialization.fail-on-empty-beans=false

# ?? Server ?????????????????????????????????????????????????????????????????
server.port=8080

# ?? JWT ????????????????????????????????????????????????????????????????????
teamFinder.jwtSecret=mersi
teamFinder.jwtExpirationMs=1000008800


python.cv.base-url=http://localhost:8001
spring.jackson.property-naming-strategy=SNAKE_CASE

